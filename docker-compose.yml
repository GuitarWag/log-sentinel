services:
  ministack:
    image: ministackorg/ministack:latest
    ports:
      - "4566:4566"
    environment:
      PERSIST_STATE: "1"
      STATE_DIR: /tmp/ministack-state
    volumes:
      - ministack-state:/tmp/ministack-state

volumes:
  ministack-state:
